services:
  app:
    build:
      context: .
      args:
        VITE_API_BEARER_TOKEN: ${VITE_API_BEARER_TOKEN:-}
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      DATABASE_URL: sqlite+aiosqlite:////app/data/app.db
    volumes:
      - app-data:/app/data

volumes:
  app-data:
